-- Custom colorscheme matching the terminal palette
return {
  {
    "LazyVim/LazyVim",
    opts = {
      -- Use minimal built-in colorscheme, we'll override with custom colors
      colorscheme = "habamax",
    },
  },
}
