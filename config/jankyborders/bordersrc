#!/bin/bash

# Load theme colors (generated by colors.sh)
source "$(dirname "$0")/theme/colors"

options=(
	style=round
	width=6.0
	hidpi=off
	active_color=$BORDER_ACTIVE
	inactive_color=$BORDER_INACTIVE
)

borders "${options[@]}"